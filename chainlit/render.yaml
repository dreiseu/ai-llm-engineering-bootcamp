services:
  - type: web
    name: chainlit-echo-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: chainlit run app.py --port $PORT --host 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0